# 更新日志

## v0.3.0 · 活着的工作桌面

这是一次完整的体验更新。慢慢赚钱仍然首先是本地计薪器，但现在也能让工作中的人看见当下的天空，并给一段专注时间留下不带评分的回望。

### 新增

- 可选 BYOK 一句话设置：使用自己配置的兼容 OpenAI API，将自然语言转换为待确认的工作设置。
- Windows DPAPI 加密保存 API Key；支持断开连接并清除本机 AI 数据。
- 默认每月 20 次 BYOK 请求上限，可自行调整为不限。
- 可选天空层：手动选择城市后，每 15 分钟同步一次天气；不读取设备定位。
- 25 分钟专注回望：记录“推进”“维持节奏”或“好好休息”，不生成效率分数。
- 发布页同时提供免安装 `.exe` 与 ZIP 下载包。

### 改进

- 新安装默认按每月 22 个计薪工作日计算。
- 设置保存后立即刷新金额、进度与黄金等值。
- 百分比不再显示多余前导零。
- 本地存储进一步拆分：薪资、AI、天空、专注记录各自独立，便于清理。

### 隐私与边界

- 默认离线。BYOK 与天气必须由用户主动开启。
- AI 只接收用户当次填写的文本；天气只接收用户手动选定城市的坐标。
- 时间价值是本地估算，不是绩效评分、投资建议或提薪证明。

## v0.2.0

- 透明冷色桌面浮层、静态黄金等值与更直观的设置保存体验。
